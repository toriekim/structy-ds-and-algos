export class Node {
  constructor(val) {
    this.val = val;
    this.next = null;
  }
}

// Iterative
// Time Complexity: O(n) -> n = # of nodes
// Space Complexity:

// Recursive
// Time Complexity: O(n) -> n = # of nodes
// Space Complexity: O(n) -> n recursive calls on stack
